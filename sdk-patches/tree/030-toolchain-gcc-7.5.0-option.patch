--- a/toolchain/gcc/Config.version
+++ b/toolchain/gcc/Config.version
@@ -18,6 +18,10 @@
 	default y if (!TOOLCHAINOPTS && !arc)
 	bool
 
+config GCC_VERSION_7_5_0
+	default n if (!TOOLCHAINOPTS && !arc)
+	bool
+
 config GCC_VERSION
 	string
 	default "linaro-5.3-2016.05" if GCC_VERSION_5_3_1
@@ -25,3 +29,4 @@
 	default "linaro-5.2-2015.11-2" if GCC_VERSION_5_2_LINARO
 	default "linaro-6.4-2017.11" if GCC_VERSION_6_4_LINARO
 	default "linaro-7.4-2019.02" if GCC_VERSION_7_4_LINARO
+	default "7.5.0" if GCC_VERSION_7_5_0
--- a/toolchain/gcc/Config.in
+++ b/toolchain/gcc/Config.in
@@ -30,6 +30,11 @@
 		select GCC_VERSION_7_4_LINARO
 		bool "gcc 7.4.1"
 		depends on !arc
+
+	config GCC_USE_VERSION_7_5_0
+		select GCC_VERSION_7_5_0
+		bool "gcc 7.5.0 (GNU release, OpenWrt 19.07 patch set; stand-in for the unobtainable Linaro 7.4 source)"
+		depends on !arc
 endchoice
 
 config GCC_USE_GRAPHITE
--- a/toolchain/gcc/common.mk
+++ b/toolchain/gcc/common.mk
@@ -77,6 +77,12 @@
   PKGVERSION=OpenWrt GCC $(PKG_VERSION) $(REVISION)
 endif
 
+ifeq ($(CONFIG_GCC_VERSION),"7.5.0")
+  PKG_SOURCE:=$(PKG_NAME)-$(PKG_VERSION).tar.xz
+  PKG_MD5SUM:=79cb8a65d44dfc8a2402b46395535c9a
+  HOST_BUILD_DIR:=$(COMPILE_DIR_TOOLCHAIN)/$(GCC_DIR)
+endif
+
 ifeq ($(CONFIG_GCC_VERSION),"4.5-2010.09")
   PKG_MD5SUM:=b4e1f518748c51b792e77a5eb9020e8b
   PKG_VERSION:=4.5.1
@@ -146,6 +152,12 @@
 		--with-mpc=$(TOPDIR)/out/host \
 		--disable-decimal-float
 
+ifeq ($(CONFIG_GCC_VERSION),"7.5.0")
+  PKG_SOURCE:=$(PKG_NAME)-$(PKG_VERSION).tar.xz
+  PKG_MD5SUM:=79cb8a65d44dfc8a2402b46395535c9a
+  HOST_BUILD_DIR:=$(COMPILE_DIR_TOOLCHAIN)/$(GCC_DIR)
+endif
+
 ifeq ($(CONFIG_GCC_VERSION),"4.5-2010.09")
   GCC_CONFIGURE += --enable-extra-sgxxlite-multilibs
 endif
